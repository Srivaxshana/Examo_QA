name: Java CI/CD Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-backend:
    runs-on: ubuntu-latest
    
    defaults:
      run:
        working-directory: examo-backend/examo-backend
    
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: sri1708
          MYSQL_DATABASE: examo_db
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h localhost -u root -psri1708"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5
    
    env:
      SPRING_DATASOURCE_URL: jdbc:mysql://localhost:3306/examo_db
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: sri1708
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven
      
      - name: Wait for MySQL to be ready
        run: |
          echo "Waiting for MySQL to be ready..."
          for i in {1..30}; do
            if mysqladmin ping -h 127.0.0.1 -P 3306 -u root -psri1708 --silent; then
              echo "✅ MySQL is ready!"
              break
            fi
            echo "⏳ MySQL not ready yet. Attempt $i/30"
            sleep 2
          done
<<<<<<< HEAD

      # Install wait-on globally
      - name: Install wait-on
        run: npm install -g wait-on

      # Install Google Chrome for Selenium
      - name: Install Google Chrome
        run: |
          sudo apt-get update
          sudo apt-get install -y wget gnupg unzip
          wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
          echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" | sudo tee /etc/apt/sources.list.d/google-chrome.list
          sudo apt-get update
          sudo apt-get install -y google-chrome-stable


      # Build backend
      - name: Build backend (Spring Boot)
        run: mvn clean install -DskipTests
        working-directory: examo-backend/examo-backend

      # Start backend and keep logs
      - name: Run backend
=======
      
      - name: Verify database connection
>>>>>>> f6a4865ead8a73056e04924273965b8d87d6c731
        run: |
          mysql -h 127.0.0.1 -P 3306 -u root -psri1708 -e "SHOW DATABASES;"
      
      # Run tests excluding Selenium/UI tests
      - name: Run unit and integration tests (excluding Selenium)
        run: mvn test -Dtest='!*Selenium*,!*UI*,!*UITest*,!LoginUITest'
      
      - name: Build package
        run: mvn package -DskipTests
      
      - name: Upload test reports
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: backend-test-reports
          path: |
            examo-backend/examo-backend/target/surefire-reports/
            examo-backend/examo-backend/target/cucumber-reports/

  build-frontend:
    runs-on: ubuntu-latest
    
    defaults:
      run:
        working-directory: examo-frontend
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: examo-frontend/package-lock.json
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build frontend
        run: CI=false npm run build
      
      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: frontend-build
          path: examo-frontend/dist/
